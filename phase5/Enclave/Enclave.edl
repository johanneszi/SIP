enclave {
    include "/home/team6/SIP/phase5/snakeApp/snake.h"
    include "sgx_tseal.h"

    trusted {
        public int collide_gold([in] snake_t *snake, [in] screen_t *screen); 
	public int collision([in] snake_t *snake, [in] screen_t *screen);

        public void load_high_score([in, size=sealed_size] uint8_t *sealed_data, size_t sealed_size);
        public void dump_high_score([out, size=sealed_size] uint8_t *sealed_data, size_t sealed_size);
        public int get_high_score();

        public int get_score();
        public void update_score_gold(int obstacles);
        public void update_score_level(int level);
        public void reset_score();

        public int get_length();
        public void increase_length();
        public void set_length(int level);

        public int get_speed();
        public void reset_speed();
        public void increase_speed(int level);
    };

    untrusted {
        void ocall_print([in, string]const char* str, int input);
    };
};
